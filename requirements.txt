# Основная библиотека для работы с Telegram API
pyrogram[asyncio]

# Асинхронный драйвер для PostgreSQL (для Neon.tech)
asyncpg

# Асинхронный веб-сервер для приёма вебхуков
aiohttp

# (Опционально, но рекомендуется для производительности)
# Ускоряет асинхронные операции
uvloop

# (Опционально, для локальной разработки)
# Позволяет загружать переменные окружения из файла .env
python-dotenv
